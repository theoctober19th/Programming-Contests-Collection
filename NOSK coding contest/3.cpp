#include <iostream>
#include <stdio.h>

using namespace std;

int main(){

	char string[10000];
	cin >> string;

	for(int i=0; string[i] != '\n'; i++){
		if(string[i] >= '0' && string[i] <= '9'){
			
		}
	}

	return 0;
}